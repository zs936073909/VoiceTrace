PySide6>=6.6.0
numpy>=1.24.0
librosa>=0.10.0
soundfile>=0.12.0
webrtcvad>=2.0.10
pydub>=0.25.1
scikit-learn>=1.3.0
reportlab>=4.0.0
pytest>=7.4.0
pyinstaller>=6.0.0

# 台风训练（可选依赖：首次使用台风训练功能时按提示安装）
mediapipe>=0.10.14
opencv-python>=4.8.0

# 韵律分析（基于 Praat 的基频、共振峰、声音质量分析）
praat-parselmouth>=0.4.7

# 强制对齐（字/词级时间戳，基于 faster-whisper）
# 首次运行时会自动下载 base 模型（约 74MB）
faster-whisper>=1.0.0
torch>=2.0.0

# AI 文案写作（可选依赖：使用在线 API 智能生成时需要）
requests>=2.31.0
